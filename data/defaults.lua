return {
    SideKick = {
        -- Note: Most settings are now managed through Registry (registry.lua)
        -- This file is kept for backwards compatibility with any code
        -- that references defaults.lua directly.
    },
    ['SideKick-Abilities'] = {
        -- Ability toggles/modes are seeded dynamically from discovered class abilities.
    },
}
